<!DOCTYPE html>

<html data-bs-theme="dark">
    <head>
        <title>header</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <link href="/styles/login.css" rel="stylesheet">
        <link href="/styles/register.css" rel="stylesheet">
        <link href="/styles/header.css" rel="stylesheet">
        <link href="/styles/footer.css" rel="stylesheet">
        <link href="/styles/notes.css" rel="stylesheet">
        <link href="/styles/note.css" rel="stylesheet">
        <link href="/styles/noteEditor.css" rel="stylesheet">
    </head>

    <body>
        <% if (locals.darkmode) { %>
        <nav class="navbar navbar-expand-lg navbar-dark bg-secondary" id="nav">
        <% } else { %>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark" id="nav">
        <% } %>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarText">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link text-white" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-white" href="/notes">Notes</a>
                    </li>
                    <% if (user) { %>
                        <li class="nav-item" id="log">
                            <a class="nav-link text-white" href="/logout">logout</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item" id="log">
                            <a class="nav-link text-white" href="/login">Login</a>
                        </li>
                        <li class="nav-item" id="register">
                            <a class="nav-link text-white" href="/register">Register</a>
                        </li>
                    <% } %>
                    <li>
                        <% if (locals.darkmode) { %>
                            <button type="button" onclick="switchMode()" class="btn btn-secondary" id="dark"><img src="/SVGs/dark-moon.svg" id="moon"></button>
                        <% } else { %>
                            <button type="button" onclick="switchMode()" class="btn btn-dark" id="dark"><img src="/SVGs/light-moon.svg" id="moon"></button>
                        <% } %>
                    </li>
                </ul>
            </div>
        </nav>
        <form id="darkmode" style="display: none;" action="/darkmode" method="post"></form>